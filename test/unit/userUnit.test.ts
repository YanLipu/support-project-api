import supertest from 'supertest'
import app from '../../src/server'
import connection from '../../dbconn/connection'

describe('Teste unitário do controller de usuário', () => {
  it('Log pra teste', () => {
    console.log('Funcionou')
  })
})
